<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net9.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
        <!-- WICHTIG: libdestoy.so direkt in das Output-Root kopieren,
             damit DllImport("libdestoy") sie findet. -->
        <None Include="lab02_toy_des_v2/libdestoy.so">
            <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
            <TargetPath>libdestoy.so</TargetPath>
        </None>
    </ItemGroup>

</Project>
